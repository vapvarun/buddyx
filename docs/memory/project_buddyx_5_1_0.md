---
name: BuddyX 5.1.0 active branch state
description: Branch state, ship-blockers, and continuation context for the 5.1.0 Kirki-removal release on `5.1.0` branch of wbcomdesigns/buddyx
type: project
originSessionId: 101e1337-3bd5-4c7f-9bd1-bdb39c4962c2
---
**Branch:** `5.1.0` on `wbcomdesigns/buddyx` (dev) — vapvarun/buddyx is release-only mirror, no dev pushes there.
**Working dir:** `/Users/varundubey/Local Sites/buddyx/app/public/wp-content/themes/buddyx`
**Latest commit:** `79e0bde` (visitor color-mode toggle — light/dark/auto cycle, 2026-05-07). Completes the dark-mode story for 5.1.0 by shipping the visitor-side toggle button that pairs with the existing token plumbing. Customizer enables/disables (`site_color_mode_toggle_show`) + position (`site_color_mode_toggle_position`: header/mobile_only/both). localStorage persistence works for guests AND members. CLS = 0. Plan: `plans/2026-05-07-visitor-color-mode-toggle.md`. Removed the half-cooked state where the customizer description literally said "if you add one".

**Why:** Original goal was Kirki removal (Kirki morphed into a page-builder plugin). Scope expanded per stakeholder direction: every premium feature must ship in 5.1.0, fully cooked. **Palettes (color + font) are deferred to 5.2.0** — that's the only acknowledged deferral.

**How to apply:** When resuming, this branch is mid-flight. The 5.1.0 execution queue lives in [5.1.0 execution queue](project_buddyx_5_1_0_execution_queue.md). Do not start new strategic features without confirming with stakeholder; finish the queue first.

## Already shipped on 5.1.0 (28+ commits)

- Kirki replacement: `inc/Customizer_Framework/` (PSR-4, 12 custom controls + 8 core types). Replaces all `\Kirki\Field\X` usage with `Field::add('type', $args)`.
- 13 audit bugs fixed (numbered in `plans/2026-05-04-customizer-options-audit.md`). Most critical: Color iris regression (#7), Typography defaults blank (#8), foreign-sub-key data-loss on save (#11), Custom-HTML wp_kses too strict (#13).
- Premium Typography control: 5-row paired layout, 9 inputs, font-style/text-align/text-decoration newly exposed.
- Site Loader expansion: 7 fields, 5 animation types, accessibility (`role=status`, `aria-live`, `prefers-reduced-motion`).
- Tokens system Phase 1+2: `inc/Tokens/Component.php` emits `--bx-color-*` tokens with backward-compat aliases for `--color-*`/`--button-*`/`--global-*` legacy names. Dark mode infrastructure: `:root[data-bx-mode="dark"]` block + `prefers-color-scheme` media query + FOUC-prevention `<head>` script. `assets/css/bx-tokens-applied.css` maps tokens to standard theme selectors.
- `sanitize_bool_int` accepts `'on'/'yes'/'true'/'enable'` (closes data-loss path on `site_custom_colors`/`site_breadcrumbs`/`buddypress_avatar_style`).
- Site Skin Phase 3 (commit `43f9a00`): 45 color fields organized into 9 visual clusters with Lucide-style SVG icon heads + 3 Header sub-cluster heads. `site_primary_color` promoted to its own "Brand" cluster; `site_sub_header_typography[color]` relocated to Header. 6 existing `custom-*-divider` defaults repurposed; 3 new cluster + 3 new sub-cluster dividers added. wp_kses whitelist in `Custom_HTML.php` extended for inline SVG. Inventory: site_skin_section 45 → 51 (+6 UI-only dividers, no value-shape changes; audit Pass 1-3 sub-checks stay green).
- Typography defaults modernization (commit `d425bf1`): 9 typography defaults updated to modern editorial scale. h1-h6 use Newsreader, menu/sub_menu/body use Inter. Body baseline 17px / 1.65 (NYT/Medium convention). Perfect-fourth size scale, line-height curve tightening on big text, letter-spacing curve negative on hero / positive on small. Defaults only affect new installs (customer-saved theme_mods take precedence via merge-over-defaults).
- Site Skin Phase 4 (commits `9ead513` 4a / `d226855` 4b / `a2eb088` 4c / `8640fa2` 4d, all 2026-05-04): full tokenization pass across 27 stylesheets — 180 `var(--bx-color-*)` references in built min.css (54 bg-elevated, 45 border, 44 fg-muted, 14 divider, 11 fg, 10 bg-muted, 2 menu-fg). 4 new framework tokens (`--bx-color-fg-muted/-border/-divider/-shadow`) emit unconditionally. `--global-border-color` (218 legacy refs in plugin compat) now aliased to `var(--bx-color-border)` so dark mode auto-inherits. theme.json palette helpers reference WP preset vars for proper dark-mode propagation. State/brand colors preserved (BP/LMS/Woo/Dokan/AMP/Youzify pastels + presence rainbow + #fff text on accents + warning/error/success states + yellow highlight). Build pipeline: src/*.css → assets/css/*.min.css via `npm run build:css`. Dark mode now coherent across foundation + community + commerce + LMS surfaces. LearnDash dark-mode.css refactor moved to 5.2.x backlog (independent subsystem).
- Token taxonomy + Phase 2 hex migration (commits `216afd6`, 2026-05-07): 96-token `_bx-tokens.css` canonical partial imported only by global.css + editor source (avoids cascade pollution). 31 hex → `var(--token, #legacy-hex)` migrations in admin/theme-settings (10), buddypress (6), buddyx-youzify (4), platform (1). Token default fix: `--bx-color-presence-online` `#16a34a` → `#4caf50` (Material green, master parity). Deferred (color-drift risk): platform status hues, WPJobManager job-type chips, iOS rainbow gradient, editor #505050 body text. Audit-before-commit pattern: every migration's resolved color compared to master legacy hex; deltaE >5 reverted or default-shifted.
- Body-BG cascade fix (commits `e9adbc0` + `182a3be`, 2026-05-07): bx-tokens-applied.css card-surface rule was scoping `.post, .page, .hentry, .entry-content, .widget, ...` to `--bx-color-bg-elevated`. `.post` and `.page` are also added to `<body>` by WP on single-post / page templates, so the rule was hijacking `body_background_color` on every static page. Dropped `.post`/`.page` from selector list (`.hentry` is universal on articles and never appears on body). Audited entire src/ tree for similar greedy selectors — only 1 other found (`front-page.css:21 .post` inside `@media --sidebar-query`, but body never has `.post` on front-page templates so it's safe in practice). Customer parity stress test: 73/73 passes (37 tokens + 36 legacy aliases reach customer-saved values byte-identical).
